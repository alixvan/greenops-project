# Frontend GreenOps

Interface React/Vite de la plateforme GreenOps.

Commandes locales :

```bash
npm ci
npm run dev
npm run lint
npm run build
```

En production Docker, le build statique est servi par Nginx.
